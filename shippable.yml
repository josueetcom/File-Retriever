language: java

jdk:
- oraclejdk8

services:

env:

matrix:

build:
  pre_ci:

  pre_ci_boot:
    image_name:
    image_tag:
    pull:
    options:
  
  ci:
    - gradle assemble
    - gradle test
  on_success:
    - cp -r app/build/test-results/* shippable/testresults
    - echo "success!"
  on_failure:
    - cp -r app/build/test-results/* shippable/testresults
    - echo "failure"
  cache:
  push:
